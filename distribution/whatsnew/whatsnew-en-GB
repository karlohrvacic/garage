• Invite by link: send it, they tap it, they are in — no code to read out
• Vignettes: pick the country, get its real periods, and a reminder the day it runs out
• Links straight to the official seller (DARS, ASFINAG, NÚSZ…), not a reseller
• Comprehensive insurance now renews like the mandatory policy
• Loading sample data shows it is working instead of looking stuck
• Fix: the + button no longer covers the last row of a list
• Fix: older entries show the year, so lists read in order
